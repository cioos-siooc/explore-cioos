#!/usr/bin/env python

from distutils.core import setup

setup(name='downloader_scheduler',
      version='0.1',
      description='',
      url='',
      packages=['downloader_scheduler'],
      install_requires=[
            'sqlalchemy',
            'psycopg2',

      ]
      )
