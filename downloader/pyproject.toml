[project]
name = "downloader"
version = "0.1.0"
description = "CIOOS Explorer download scheduler service."
readme = "README.md"
requires-python = "~=3.10.0"
dependencies = [
    "erddapy>=2.2.4",
    "pandas<2.0.0",
    "pdfkit>=1.0.0",
    "shapely>=2.1.1",
]
