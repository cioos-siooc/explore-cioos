#!/usr/bin/env python

from distutils.core import setup

setup(
    name="erddap_estimate",
    version="0.1",
    description="",
    url="",
    packages=["erddap_estimate"],
    install_requires=[
        "pandas",
    ],
)
