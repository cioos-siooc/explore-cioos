/var/log/nginx/*.log {
  su root root
daily
  missingok
  rotate 14
  dateext
  compress
  delaycompress
  notifempty
  sharedscripts
  postrotate
    [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
  endscript
}
