"""Neutral foundations shared by the harvester, the downloader and the scheduler.

Submodules are imported explicitly (``from cde_common.db import ...``) rather
than re-exported here, so importing one costs only what it needs.
"""
